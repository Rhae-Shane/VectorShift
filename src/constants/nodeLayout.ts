/** Shared node dimensions (keep in sync with --vs-node-* CSS vars). */
export const NODE_DEFAULT_WIDTH = 230;
export const NODE_DEFAULT_MAX_WIDTH = 290;
export const NODE_INPUT_WIDTH = 250;
export const NODE_OUTPUT_WIDTH = 290;

export const TEXT_NODE_MIN_WIDTH = 200;
export const TEXT_NODE_MAX_WIDTH = 290;
export const TEXT_NODE_MIN_HEIGHT = 72;
export const TEXT_NODE_MIN_TEXTAREA_HEIGHT = 28;
export const TEXT_NODE_MAX_TEXTAREA_HEIGHT = 120;
export const TEXT_NODE_FIELD_HORIZONTAL_PAD = 36;
export const TEXT_NODE_VERTICAL_CHROME = 56;

export const TEXT_NODE_MAX_HEIGHT =
  TEXT_NODE_MAX_TEXTAREA_HEIGHT + TEXT_NODE_VERTICAL_CHROME;

/** Node header icon size — keep in sync across registry + NodeHeader. */
export const NODE_HEADER_ICON_SIZE = 18;
